### Docker compose configuration for exposing scenario stats to prometheus

#### Minimal setup
Create the `.env` file from template and adjust according to the server settings.
```
mv .env_template .env
```
